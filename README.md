# 校园二手物品智能匹配平台

基于多特征融合推荐算法的校园二手物品交易与智能匹配平台。

## 项目简介

本平台是一个面向高校学生的二手物品交易与智能匹配平台。不同于传统商城系统，平台支持用户发布闲置商品、发布求购需求，并基于**多特征融合推荐算法**，从商品分类、关键词、价格、新旧程度、交易地点、用户行为、商品热度等多个维度，自动计算商品与求购需求的匹配度，直观展示匹配分数和匹配原因，提高校园二手交易的效率与体验。

## 技术栈

| 模块 | 技术 |
|------|------|
| 后端 | Java 17 + Spring Boot + MyBatis-Plus + MySQL 8 + JWT + Maven + Lombok |
| Web 用户端 | Vue 3 + Vite + Element Plus + Axios + Pinia + Vue Router |
| Web 管理后台 | Vue 3 + Vite + Element Plus + Axios + Pinia + Vue Router + ECharts |
| Android 端 | 原生 Android + Java + Retrofit/OkHttp + RecyclerView + SharedPreferences |
| 部署 | Ubuntu 22.04 + Nginx + MySQL + Spring Boot Jar + 静态文件 |

## 目录结构

```
campus-secondhand-platform/
├── secondhand-backend      # 后端服务 (Spring Boot)
├── secondhand-web-user     # Web 用户端 (Vue 3)
├── secondhand-web-admin    # Web 管理后台 (Vue 3)
├── secondhand-android      # Android 用户端
├── sql                     # 数据库脚本
├── docs                    # 项目文档
├── deploy                  # 部署相关配置
└── README.md
```

## 开发阶段

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段一 | 项目初始化与规范文档 | ✅ 已完成 |
| 阶段二 | 数据库设计 | ✅ 已完成 |
| 阶段三 | 后端基础框架搭建 | ✅ 已完成 |
| 阶段四 | 登录认证 + 权限控制 + 图片上传 | ✅ 已完成 |
| 阶段五 | 商品模块开发 | ✅ 已完成 |
| 阶段六 | 求购模块开发 | ✅ 已完成 |
| 阶段七 | 收藏+交易意向+搜索浏览历史 | ✅ 已完成 |
| 阶段八 | 管理员商品审核与下架 | ✅ 已完成 |
| 阶段九 | 管理员求购审核 | ✅ 已完成 |
| 阶段十 | 管理员用户管理 | ✅ 已完成 |
| 阶段十一 | 管理员订单查看与平台统计 | ✅ 已完成 |
| 阶段十二 | 智能匹配与推荐算法 | ✅ 已完成 |
| 阶段十三 | Web 用户端基础框架与登录注册 | ✅ 已完成 |
| 阶段十四 | Web 用户端商品首页、列表、详情 | ✅ 已完成 |
| 阶段十五 | Web 用户端发布商品与我的商品 | ✅ 已完成 |
| 阶段十六 | Web 管理后台开发 | ⏳ 待开始 |
| 阶段十七 | Android 端开发 | ⏳ 待开始 |
| 阶段十八 | 部署与测试 | ⏳ 待开始 |

## 快速开始

### 1. 数据库初始化

```bash
mysql -u root -p < sql/init.sql
```

### 2. 后端启动

```bash
cd secondhand-backend

# 使用 Maven Wrapper（推荐）
./mvnw spring-boot:run

# 或使用本地 Maven
mvn spring-boot:run
```

启动后访问：`http://localhost:8080/api/health`

### 3. Web 用户端启动

```bash
cd secondhand-web-user

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

启动后访问：`http://localhost:5173`

**前端技术栈**：Vue 3 + Vite + Element Plus + Vue Router + Pinia + Axios

**默认测试账号**：student001 / 123456

**已实现功能**：
- 用户登录/注册
- 首页商品展示（最新商品）
- 商品列表（搜索、分类筛选、成色筛选、价格区间、分页）
- 商品详情（图片浏览、卖家信息、联系方式）
- 发布商品（图片上传、表单填写）
- 我的商品（状态筛选、修改、删除）
- 求购广场（搜索、分类筛选、预算区间、分页）
- 求购详情（分类、预算、期望成色、地点、关键词、发布者信息）
- 发布求购（表单提交，成功后等待审核）
- 我的求购（状态筛选、查看详情、修改、删除）
- 商品收藏（详情页收藏/取消收藏）
- 我的收藏（卡片列表、取消收藏、分页）
- 交易意向（详情页发起购买意向、填写留言）
- 我的交易（购买意向/出售意向、状态流转：接受/拒绝/完成/取消）
- 智能推荐（首页个性化商品推荐，匹配分数和推荐原因）
- 相似商品推荐（商品详情页下方，基于分类/价格/关键词推荐）
- 智能匹配商品（根据求购匹配商品，显示匹配分数和原因）
- 智能匹配求购（根据商品匹配求购，显示匹配分数和原因）

### 4. 配置说明

数据库连接配置在 `secondhand-backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/campus_secondhand
    username: root
    password: root   # 根据本地 MySQL 修改
```

---

## 登录认证说明

### 测试账号

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | admin | 123456 |
| 普通用户 | student001 | 123456 |
| 普通用户 | student002 | 123456 |

> 密码使用 BCrypt 加密存储，数据库中的密码字段为密文，不可直接修改。

### Token 使用方式

需要登录的接口在请求头中携带 Token：

```
Authorization: Bearer <token>
```

### 加密方式

- 密码使用 BCrypt 加密存储
- Token 使用 JWT（HS512），包含 userId、username、userType 信息
- Token 默认有效期 24 小时

### 图片上传目录说明

- 开发环境上传目录：`secondhand-backend/upload/`
- 生产环境上传目录：`/www/upload/`
- 按日期分目录存储，例如 `/upload/2026/06/xxx.jpg`
- 文件名使用 UUID，避免重名
- 支持 jpg、jpeg、png、webp 格式，限制 5MB

可在 `application.yml` 中配置：
```yaml
file:
  upload-path: ./upload/      # 上传目录
  access-prefix: /upload/     # 访问前缀
```

---

## 求购功能测试方法

### 1. 发布求购

1. 使用 `student001 / 123456` 登录
2. 点击导航栏「发布」下拉或直接访问 `/publish-demand`
3. 填写求购表单：标题、分类、描述、预算范围、期望成色、期望地点、关键词
4. 点击「发布求购」
5. 应提示「发布成功，等待管理员审核」
6. 自动跳转到 `/my-demands`，列表中应有刚发布的求购，状态为「待审核」

### 2. 求购列表

1. 在管理员后台审核通过一条求购后
2. 访问 `/demand` 页面，已通过的求购应出现在列表中
3. 测试搜索框输入关键词，列表应更新
4. 测试分类筛选下拉框
5. 测试预算区间筛选
6. 测试分页功能
7. 未登录用户也可以正常访问求购列表

### 3. 求购详情

1. 在求购列表点击任意卡片，进入 `/demand/:id`
2. 应显示：标题、分类、预算范围、期望成色、期望地点、关键词、发布者、发布时间、详细描述
3. 点击「返回」按钮可回到列表

### 4. 我的求购

1. 登录后访问 `/my-demands` 或点击导航栏「我的求购」
2. 应显示当前用户发布的所有求购
3. 测试状态筛选：全部、待审核、已通过、已驳回、已关闭
4. 点击「查看」跳转到求购详情页
5. 点击「修改」弹出修改弹窗，回显已有数据，修改后保存
6. 点击「删除」弹出确认框，确认后删除成功并刷新列表

### 5. 修改求购

1. 在我的求购列表点击「修改」按钮
2. 弹窗中应回显已有数据（标题、分类、描述、预算、成色、地点、关键词）
3. 修改任意字段后点击「确认修改」
4. 应提示「修改成功」，列表刷新
5. 修改后求购状态可能变回「待审核」，这是正常行为

### 6. 删除求购

1. 在我的求购列表点击「删除」按钮
2. 弹出确认框「确定要删除这条求购吗？」
3. 点击「确认删除」，提示「删除成功」
4. 列表刷新，该求购不再显示

### 7. 未登录拦截

1. 未登录状态下访问 `/publish-demand`，应自动跳转到 `/login`
2. 未登录状态下访问 `/my-demands`，应自动跳转到 `/login`

---

## 收藏与交易功能测试方法

### 1. 商品收藏

1. 使用 `student001 / 123456` 登录
2. 进入任意商品详情页 `/goods/:id`（确保不是自己发布的商品）
3. 点击「收藏」按钮
4. 应提示「收藏成功」，按钮变为「已收藏」
5. 自己发布的商品不显示收藏按钮

### 2. 取消收藏

1. 在已收藏的商品详情页，点击「已收藏」按钮
2. 应提示「已取消收藏」，按钮恢复为「收藏」
3. 或在 `/favorites` 页面点击「取消收藏」
4. 提示「已取消收藏」，列表刷新

### 3. 我的收藏

1. 登录后访问 `/favorites` 或点击导航栏「收藏」
2. 应显示已收藏的商品卡片（图片、标题、价格、成色、收藏时间）
3. 点击卡片进入商品详情页
4. 测试分页功能
5. 空收藏时显示空状态提示

### 4. 创建交易意向

1. 登录后进入他人商品的详情页 `/goods/:id`
2. 点击「我想要」按钮
3. 弹出输入框，填写留言
4. 点击「提交意向」
5. 应提示「交易意向已提交，请等待卖家处理」
6. 自己发布的商品不显示「我想要」按钮

### 5. 我的购买意向

1. 登录买家账号，访问 `/my-orders`
2. 默认显示「我的购买」Tab
3. 应显示已提交的购买意向（商品标题、价格、卖家、留言、状态、时间）
4. `pending` 状态的意向可点击「取消」
5. `accepted` 状态的意向可点击「确认完成」
6. 测试状态筛选：全部/待处理/已接受/已拒绝/已完成/已取消

### 6. 我的出售意向

1. 登录卖家账号，访问 `/my-orders`
2. 切换到「我的出售」Tab
3. `pending` 状态的意向可点击「接受」或「拒绝」
4. `accepted` 状态的意向可点击「确认完成」

### 7. 交易状态流转

1. 买家 A 在商品详情页提交购买意向 → 状态为 `pending`
2. 卖家 B 在「我的出售」中点击「接受」→ 状态变为 `accepted`
3. 卖家 B 在「我的出售」中点击「确认完成」→ 状态变为 `completed`
4. 或者：卖家 B 在「我的出售」中点击「拒绝」→ 状态变为 `rejected`
5. 或者：买家 A 在「我的购买」中点击「取消」→ 状态变为 `cancelled`

### 8. 未登录拦截

1. 未登录状态下访问 `/favorites`，应自动跳转到 `/login`
2. 未登录状态下访问 `/my-orders`，应自动跳转到 `/login`
3. 未登录状态下在详情页点击「收藏」或「我想要」，应跳转 `/login`

---

## 智能推荐功能测试方法

### 1. 首页智能推荐

1. 使用 `student001 / 123456` 登录
2. 访问首页 `/`
3. 在「核心功能」和「最新商品」之间应看到「智能推荐」区域
4. 标题为"智能推荐"，副标题描述推荐生成来源
5. 推荐卡片右上角显示推荐分数（如"86分"）
6. 推荐卡片底部显示推荐原因标签（如"分类相近""价格相近"）
7. 点击推荐卡片跳转到对应商品详情页

### 2. 未登录状态推荐区域

1. 退出登录或使用无痕窗口访问首页 `/`
2. 「智能推荐」区域显示引导卡片
3. 提示"登录后可查看个性化智能推荐"
4. 点击「立即登录」跳转 `/login`

### 3. 空推荐数据

1. 使用没有行为数据的新账号登录
2. 后端返回热门商品作为兜底
3. 如果后端返回空数组，显示空状态提示

### 4. 商品详情页相似推荐

1. 进入任意商品详情页 `/goods/:id`
2. 在商品描述下方应看到「相似商品推荐」区域
3. 展示相似商品卡片（最多6个）
4. 点击相似商品卡片跳转到对应详情页
5. 切换到另一个商品详情页时，相似推荐应随之更新

### 5. 推荐分数和推荐原因展示

- 推荐卡片右上角 `score-badge` 显示绿色渐变分数标签
- 推荐原因以青色边框小标签展示在价格下方
- 如果接口未返回 `recommendScore` 或 `recommendReasons`，卡片正常展示不报错

---

## 智能匹配功能测试方法

### 1. 根据求购匹配商品

1. 使用 `student001 / 123456` 登录
2. 访问 `/my-demands`，对一条已通过的求购点击「智能匹配商品」
3. 进入 `/match/demand/:id` 页面
4. 应显示匹配商品卡片列表，每张卡片展示：
   - 商品图片、标题、价格、成色
   - 左侧 SVG 环形匹配分数（如 86%）
   - 匹配原因标签（如"分类相近""价格匹配"）
5. 点击「查看商品」跳转到对应商品详情页
6. 匹配结果为空时显示空状态提示

### 2. 根据商品匹配求购

1. 登录后访问 `/my-goods`，对一条已上架商品点击「匹配求购」
2. 进入 `/match/goods/:id` 页面
3. 应显示匹配求购卡片列表，每张卡片展示：
   - 求购标题、预算范围、期望成色、期望地点
   - 匹配分数环形图
   - 匹配原因标签
4. 点击「查看求购」跳转到对应求购详情页
5. 未上架商品按钮禁用，提示"商品上架后可匹配求购"

### 3. 匹配分数和匹配原因展示

- 匹配分数以 SVG 环形进度图展示，分数 0-100
- 渐变色弧线（蓝紫到青色）动态填充
- 匹配原因以青色边框标签展示（前 2 条）
- 如果无匹配原因，显示"系统根据多特征综合推荐"
- 字段兼容：matchScore / score、matchReasons / matchReason 数组或字符串